apiVersion: v1
kind: Service
metadata:
  name: {{ include "solohelm.fullname" . }}
  labels:
    {{- include "solohelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "solohelm.selectorLabels" . | nindent 4 }}
